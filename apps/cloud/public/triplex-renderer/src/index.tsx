// Stub renderer for spike 2b — proves Triplex server boots inside a WebContainer.
// Real renderer wiring lives in spike 2c.
export const bootstrap = () => async () => undefined;
export const Canvas = () => null;
